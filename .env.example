# ============================================================
# REQUIRED — the app will not start without these
# ============================================================

# Get this free from https://neon.tech (or any PostgreSQL host).
# Paste the full connection string it gives you here.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Get this free from https://upstash.com (or any Redis host).
# Paste the "Redis Connect URL" (starts with rediss://) here.
REDIS_URL="rediss://default:password@host:port"

# Any random long text — used to sign login tokens. Just make one up.
JWT_SECRET=change_me_to_something_random_and_long

JWT_EXPIRES_IN=7d
PORT=3000
NODE_ENV=production

# ============================================================
# OPTIONAL — the app runs fine without these. Each feature that
# needs one simply shows as "skipped" until you add it.
# ============================================================

# AI shopping assistant real answers (without this, a rule-based
# fallback answers instead — still works, just less conversational).
ANTHROPIC_API_KEY=

# Google / GitHub / Apple login buttons
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
APPLE_CLIENT_ID=
APPLE_CLIENT_SECRET=

# Email / SMS / WhatsApp delivery for notifications
SMTP_HOST=
SMTP_USER=
SMTP_PASS=
TWILIO_SID=
TWILIO_TOKEN=
WHATSAPP_TOKEN=

# Real Amazon / Flipkart live price data
AMAZON_PAAPI_ACCESS_KEY=
AMAZON_PAAPI_SECRET_KEY=
AMAZON_PAAPI_PARTNER_TAG=
FLIPKART_AFFILIATE_ID=
FLIPKART_AFFILIATE_TOKEN=

# Only needed if you run the local demo scraper test target
FIXTURE_STORE_URL=http://localhost:4001
